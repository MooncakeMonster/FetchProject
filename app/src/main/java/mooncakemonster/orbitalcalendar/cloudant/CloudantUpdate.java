package mooncakemonster.orbitalcalendar.cloudant;

/**
 * This class updates user details into database when required.
 */
public class CloudantUpdate {
    /*
    Database database;

    public CloudantUpdate() {
        Database database = new CloudantConnect().getDatabase("database_name");
    }

    // This method updates user's email address in Cloudant database.
    public void updateEmailAddress(String email_address) {
        User user = database.find(User.class, "");
        user.setEmail_address(email_address);

        // Update user email address in database
        Response response = database.update(user);
        Log.d("Updated email", "id: " + response.getId() + ", rev: " + response.getRev());
    }

    // This method updates user's username in Cloudant database.
    public void updateUsername(String username) {
        User user = database.find(User.class, "");
        user.setUsername(username);

        // Update username in database
        Response response = database.update(user);
        Log.d("Updated username", "id: " + response.getId() + ", rev: " + response.getRev());
    }
    */
}
